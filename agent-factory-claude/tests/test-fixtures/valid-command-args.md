---
description: "Format a JSON file"
argument-hint: "<file_path>"
allowed-tools: Read, Write, Bash
---

Read the JSON file at $ARGUMENTS, format it with proper indentation, and write it back.

If $ARGUMENTS is empty, ask the user which file to format.
